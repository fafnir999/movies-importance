# Calculating the movies importance based on culture references

### Installation

1. Create python virtual environment
2. Install requirements `pip3 install -r requirements.txt`
3. Launch notebook `jupyter notebook`